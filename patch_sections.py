import re

with open('ai/sections.py', 'r') as f:
    content = f.read()

# Add destiny_card to generate_section
new_section = """    elif section == "antitaro":
        cid = card_id if card_id else random.randint(0, 77)
        prompt = f"{base_info} Сделай ВСТУПЛЕНИЕ (2-3 предложения, максимально лаконично) и разбор ОТКРОВЕНИЕ (честный, глубокий взгляд на то, что мешает твоему счастью, освобождение от иллюзий, глубокий и подробный анализ). ОБЯЗАТЕЛЬНО используй слово ОТКРОВЕНИЕ на отдельной строке перед основным разбором. Выдели заголовки ВСТУПЛЕНИЕ и ОТКРОВЕНИЕ КАПСОМ. В самом конце текста ОБЯЗАТЕЛЬНО добавь строку с ID карты Таро в формате: ID_ТАРО: {cid}. Вплети этот ID прямо в свой прогноз.{style_instruction}"
    elif section == "destiny_card":
        prompt = f"{base_info} Сделай ВСТУПЛЕНИЕ (2-3 предложения, максимально лаконично) и разбор КАРТА СУДЬБЫ (главный жизненный путь, предназначение, кармические задачи и скрытые сильные и слабые стороны). ОБЯЗАТЕЛЬНО используй слово КАРТА СУДЬБЫ на отдельной строке перед основным разбором. Выдели заголовки ВСТУПЛЕНИЕ и КАРТА СУДЬБЫ КАПСОМ. Построй глубокий, вдохновляющий и сакральный разбор, полностью основанный на энергии выпавшей карты и дате рождения.{style_instruction}"
    elif section == "card_of_day":"""

content = re.sub(
    r'    elif section == "antitaro":.*?    elif section == "card_of_day":',
    new_section,
    content,
    flags=re.DOTALL
)

with open('ai/sections.py', 'w') as f:
    f.write(content)
